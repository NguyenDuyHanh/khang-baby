# Khang-baby Backend

Minimal Express backend with JWT auth and MySQL.

Quick start:

```powershell
cd back-end
npm install
copy .env.example .env    # Windows
# or on bash: cp .env.example .env
npm run dev
```

Configure database in `.env`.
